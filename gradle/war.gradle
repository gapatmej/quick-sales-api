apply plugin: "war"

bootWar {
    mainClassName = "ec.com.newsolutions.QuickSalesApiApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
